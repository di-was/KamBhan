// Contains variable declarations for the app

// Buttons
let newTask = document.querySelector('.')
let themeToggle = document.querySelector('.')
let hamburgerIcon = document.querySelector('.')
let sideBarVisibility = document.querySelector('.')
let newBoard = document.querySelector('.')
let boards = document.querySelectorAll('.') // Contains array of buttons

const variables  = {
    newTask, themeToggle, hamburgerIcon, sideBarVisibility, newBoard, boards
}

export default variables